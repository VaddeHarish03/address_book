an explanation on how to execute the app, including the exact
terminal commands.

# Install dependencies:

 python -m venv venv
 pip install -r requirements.txt

 Run the Application:
     uvicorn main:app --reload
